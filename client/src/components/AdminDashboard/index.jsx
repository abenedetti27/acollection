import React from 'react';

const AdminDashboard = ({ artworkData, orderData }) => {
  return (
    <div className="admin-dashboard">
      <h2>Admin Dashboard</h2>
      {/* Display tools for managing artwork, artists, and orders */}
      {/* Add tables, charts, and other admin-specific elements */}
    </div>
  );
};

export default AdminDashboard;
